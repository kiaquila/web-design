{
  "name": "chaijana-menu",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/menu.test.mjs",
    "check": "npm run build && npm test"
  }
}
