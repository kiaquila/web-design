{
  "id": "static-cloudflare",
  "description": "Static site deployed as Cloudflare Worker Static Assets.",
  "deploymentProvider": "cloudflare-workers",
  "requiredProjectFiles": ["wrangler.json"],
  "requirements": [
    "Pin compatibility_date",
    "Keep Worker names, domains, account identifiers and credentials project-owned",
    "Use preview deployments for pull requests and production only from main"
  ]
}
